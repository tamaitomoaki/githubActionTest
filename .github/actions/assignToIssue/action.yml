name: 'Assign to issue'
description: 'Assign to issue'
inputs:
  who-to-greet: # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you'
  mention:
    description: 'target'
runs:
  using: 'node12'
  main: 'index.js'
